<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MemoBaseListActivity"
    android:background="@drawable/background"
    android:orientation="vertical"
    android:padding="10dp" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="5dp"
        android:background="#55000000"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/memobaselist_btnNewMemoBase"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="5dp"
            android:layout_weight="1"
            android:background="@drawable/navigation_button"
            android:maxWidth="100dp"
            android:minHeight="40dp"
            android:text="@string/memobaselist_lblNewMemoBase"
            android:textSize="15sp" />

        <Button
            android:id="@+id/memobaselist_btnSync"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/navigation_button"
            android:enabled="false"
            android:maxWidth="100dp"
            android:minHeight="40dp"
            android:text="@string/memobaselist_lblSync"
            android:textSize="15sp" />

    </LinearLayout>
    
    <ListView
        android:id="@+id/memobaselist_lstMemo"
        android:layout_width="match_parent"
        android:layout_height="match_parent" 
        android:background="#55000000"
    android:padding="10dp" />

</LinearLayout>